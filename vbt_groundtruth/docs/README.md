# docs

The authoritative, frozen specs for this pipeline live in the **repo-root `../../docs/`**,
not here (to avoid duplication):

- `00_FOUNDATION.md` — shared contracts (types, config, schema, orchestrator)
- `M0_harness_and_set_segmentation.md` … `M3-M5_downstream.md` — per-milestone specs
- `AGENT_RUNBOOK.md` — build order
- `REPO_MAP.md` — repository map (acquisition, studio, old-algorithm scope)
- `camera_config.md` — camera/tracker capture configuration

Those documents win on any conflict. This file exists only to satisfy the §0.3 layout.
